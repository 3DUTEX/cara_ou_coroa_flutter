import 'package:flutter/material.dart';

// Para usar uma cor hexadecimal é necessário utilizar o prefixo: 0xff
// Exemplo: 0xff(hexadecimal)

// Para utilizar RGB basta acessar um método da classe Color
// Color.fromRGBO (Red, Green, Blue, Opacity)
class CustomColors {
  static Color primaryColor = Color(0xff61bd8c);
}